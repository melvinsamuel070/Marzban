# name: End-to-End Infrastructure & App Deployment

# on:
#   push:
#     branches: [ master ]

# jobs:
#   # STEP 1: Code Linting and Safety Verification
#   test-and-lint:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'

#       - name: Run Syntax Checks
#         run: |
#           pip install flake8
#           flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

#   # STEP 2: Terraform Infrastructure Provisioning
#   infrastructure:
#     needs: test-and-lint
#     runs-on: ubuntu-latest
#     outputs:
#       vps_ip: ${{ steps.get_ip.outputs.vps_ip }}
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

#       - name: Terraform Init & Apply
#         env:
#           # Satisfies all interactive prompt roadblocks
#           TF_VAR_key_name: ${{ secrets.AWS_KEY_NAME }} 
#           TF_VAR_instance_name: "marzban-production-server"
#           TF_VAR_instance_type: "t3.medium" # Injects the missing instance type parameter (adjust type if needed)
          
#           # PROVIDES SECURITY ACCESS KEYS TO AWS NATIVELY
#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
#         run: |
#           cd terraform-ec2/ 
#           terraform init
#           terraform apply -auto-approve
          
#       - name: Extract VPS Public IP
#         id: get_ip
#         env:
#           # PROVIDES SECURITY ACCESS TO THE S3 BACKEND STATE FILE
#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
#         run: |
#           cd terraform-ec2/
#           # NATIVE FIX: Run standard terraform output directly to bypass wrapper limitations
#           echo "vps_ip=$(terraform output -raw public_ip)" >> $GITHUB_OUTPUT
#   # STEP 3: Automated Docker Build
#   build-image:
#     needs: test-and-lint
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       # 1. Set up QEMU for ARM emulation capability
#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v3

#       # 2. Set up Docker Buildx (CRITICAL: This switches the driver to 'docker-container')
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       # 3. Securely authenticate with Docker Hub
#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       # 4. Compile and Push Multi-Arch Container Image
#       - name: Build and Push Multi-Arch Container
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           platforms: linux/amd64,linux/arm64 # Now this driver will accept both architectures easily!
#           tags: ${{ secrets.DOCKER_USERNAME }}/marzban:latest

#   # STEP 4: Automated Remote Server Initialization, DuckDNS Update, SSL Generation, and App Spin-up
#   deploy-and-secure:
#     needs: [infrastructure, build-image]
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Automated Bootstrapping via SSH
#         uses: appleboy/ssh-action@v1.0.3
#         env:
#           APP_DOMAIN: ${{ secrets.DOMAIN }}
#           APP_EMAIL: ${{ secrets.EMAIL }}
#           DOCKER_USER: ${{ secrets.DOCKER_USERNAME }}
#           DUCK_TOKEN: ${{ secrets.DUCKDNS_TOKEN }} 
#           VPS_IP: ${{ needs.infrastructure.outputs.vps_ip }}
#         with:
#           host: ${{ needs.infrastructure.outputs.vps_ip }}
#           username: ubuntu # Updated to ubuntu for AWS AMI compatibility
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           envs: APP_DOMAIN,APP_EMAIL,DOCKER_USER,DUCK_TOKEN,VPS_IP
#           script: |
#             # 1. Update DuckDNS to point to our newly provisioned AWS Public IP
#             SUBDOMAIN=$(echo $APP_DOMAIN | cut -d'.' -f1)
#             echo "Updating DuckDNS record for $SUBDOMAIN to point to $VPS_IP..."
#             curl -s "https://www.duckdns.org/update?domains=$SUBDOMAIN&token=$DUCK_TOKEN&ip=$VPS_IP"
            
#             # Extended delay to 45 seconds for global DNS propagation stability
#             echo "Sleeping for 45 seconds to let public DNS networks sync paths..."
#             sleep 45

#             # 2. AUTOMATED SYSTEM DEPENDENCY INJECTION
#             # Installs Docker and the Docker Compose plugin seamlessly if missing from the bare AWS AMI
#             if ! command -v docker &> /dev/null; then
#               echo "Docker engine not detected. Initializing automated installation suite..."
#               sudo apt-get update -y
#               sudo apt-get install -y ca-certificates curl gnupg
#               sudo install -m 0755 -d /etc/apt/keyrings
#               curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
#               sudo chmod a+r /etc/apt/keyrings/docker.gpg
              
#               echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
#               sudo apt-get update -y
#               sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
              
#               # Provision non-root privilege binding for the ubuntu runner service account
#               sudo usermod -aG docker ubuntu
#             fi

#             # 3. Create clean production directory layout and claim ownership from root
#             sudo mkdir -p /opt/marzban-production/nginx/conf.d
#             sudo chown -R ubuntu:ubuntu /opt/marzban-production
#             cd /opt/marzban-production

#             # 4. Write dynamic, automated Nginx bootstrap config
#             cat << EOF > nginx/conf.d/marzban.conf
#             server {
#                 listen 80;
#                 server_name $APP_DOMAIN;
#                 location /.well-known/acme-challenge/ {
#                     root /var/www/certbot;
#                 }
#             }
#             EOF

#             # 5. Write out a dynamic docker-compose execution stack (Removed obsolete version tag)
#             cat << EOF > docker-compose.yml
#             services:
#               marzban:
#                 image: $DOCKER_USER/marzban:latest
#                 restart: always
#                 ports:
#                   - "8000:8000"
#                 volumes:
#                   - /var/lib/marzban:/var/lib/marzban

#               nginx:
#                 image: nginx:alpine
#                 restart: always
#                 ports:
#                   - "80:80"
#                   - "443:443"
#                 volumes:
#                   - ./nginx/conf.d:/etc/nginx/conf.d
#                   - /etc/letsencrypt:/etc/letsencrypt
#                   - /var/www/certbot:/var/www/certbot

#               certbot:
#                 image: certbot/certbot
#                 volumes:
#                   - /etc/letsencrypt:/etc/letsencrypt
#                   - /var/www/certbot:/var/www/certbot
#                 entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait \$\${!}; done;'"
#             EOF

#             # 6. Spin up HTTP Nginx validation instance using sudo privilege
#             sudo docker compose up -d nginx
#             echo "Waiting 10 seconds for Nginx listener bindings to stabilize..."
#             sleep 10

#             # 7. Automate Certificate Request Injection with Let's Encrypt
#             sudo docker run --rm \
#               -v /etc/letsencrypt:/etc/letsencrypt \
#               -v /var/www/certbot:/var/www/certbot \
#               certbot/certbot certonly --webroot \
#               --webroot-path=/var/www/certbot \
#               -d $APP_DOMAIN --email $APP_EMAIL --agree-tos --no-eff-email --non-interactive

#             # 8. Overwrite Bootstrap config with the full Production HTTPS Reverse Proxy layout
#             cat << EOF > nginx/conf.d/marzban.conf
#             server {
#                 listen 80;
#                 server_name $APP_DOMAIN;
#                 return 301 https://\$host\$request_uri;
#             }
#             server {
#                 listen 443 ssl http2;
#                 server_name $APP_DOMAIN;
#                 ssl_certificate /etc/letsencrypt/live/$APP_DOMAIN/fullchain.pem;
#                 ssl_certificate_key /etc/letsencrypt/live/$APP_DOMAIN/privkey.pem;
                
#                 location / {
#                     proxy_pass http://marzban:8000;
#                     proxy_set_header Host \$host;
#                     proxy_set_header X-Real-IP \$remote_addr;
#                     proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
#                 }
#             }
#             EOF

#             # 9. Start everything and restart Nginx to hook into the new SSL certificates securely
#             sudo docker compose pull
#             sudo docker compose up -d --remove-orphans
#             sudo docker compose restart nginx








# name: End-to-End Infrastructure & App Deployment

# on:
#   push:
#     branches: [ master ]

# jobs:
#   # STEP 1: Code Linting and Safety Verification
#   test-and-lint:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'

#       - name: Run Syntax Checks
#         run: |
#           pip install flake8
#           flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

#   # STEP 2: Terraform Infrastructure Provisioning
#   infrastructure:
#     needs: test-and-lint
#     runs-on: ubuntu-latest
#     outputs:
#       vps_ip: ${{ steps.get_ip.outputs.vps_ip }}
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

#       - name: Terraform Init & Apply
#         env:
#           TF_VAR_key_name: ${{ secrets.AWS_KEY_NAME }}
#           TF_VAR_instance_name: "marzban-production-server"
#           TF_VAR_instance_type: "t3.medium"

#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
#         run: |
#           cd terraform-ec2/
#           terraform init
#           terraform apply -auto-approve

#       - name: Extract VPS Public IP
#         id: get_ip
#         env:
#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
#         run: |
#           cd terraform-ec2/
#           echo "vps_ip=$(terraform output -raw public_ip)" >> $GITHUB_OUTPUT

#   # STEP 3: Automated Docker Build
#   build-image:
#     needs: test-and-lint
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to Docker Hub
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Build and Push Multi-Arch Container
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           platforms: linux/amd64,linux/arm64
#           tags: ${{ secrets.DOCKER_USERNAME }}/marzban:latest

#   # STEP 4: Deployment + DuckDNS + SSL + App Bootstrapping
#   deploy-and-secure:
#     needs: [infrastructure, build-image]
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4

#       - name: Automated Bootstrapping via SSH
#         uses: appleboy/ssh-action@v1.0.3
#         env:
#           APP_DOMAIN: ${{ secrets.DOMAIN }}
#           APP_EMAIL: ${{ secrets.EMAIL }}
#           DOCKER_USER: ${{ secrets.DOCKER_USERNAME }}
#           DUCK_TOKEN: ${{ secrets.DUCKDNS_TOKEN }}
#           VPS_IP: ${{ needs.infrastructure.outputs.vps_ip }}
#         with:
#           host: ${{ needs.infrastructure.outputs.vps_ip }}
#           username: ubuntu
#           key: ${{ secrets.SSH_PRIVATE_KEY }}
#           envs: APP_DOMAIN,APP_EMAIL,DOCKER_USER,DUCK_TOKEN,VPS_IP
#           script: |

#             set -e

#             # 1. Update DuckDNS
#             SUBDOMAIN=$(echo $APP_DOMAIN | cut -d'.' -f1)

#             echo "Updating DuckDNS..."
#             curl -fsS "https://www.duckdns.org/update?domains=$SUBDOMAIN&token=$DUCK_TOKEN&ip=$VPS_IP"

#             # 2. WAIT FOR REAL DNS PROPAGATION (CRITICAL FIX)
#             echo "Waiting for DNS propagation..."

#             for i in {1..30}; do
#               CURRENT_IP=$(nslookup $APP_DOMAIN | awk '/Address: / {print $2}' | tail -n1)
#               echo "DNS resolved to: $CURRENT_IP"

#               if [ "$CURRENT_IP" = "$VPS_IP" ]; then
#                 echo "DNS is correct ✔"
#                 break
#               fi

#               sleep 10
#             done

#             # 3. Install Docker if missing
#             if ! command -v docker &> /dev/null; then
#               sudo apt-get update -y
#               sudo apt-get install -y ca-certificates curl gnupg
#               sudo install -m 0755 -d /etc/apt/keyrings

#               curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
#               sudo chmod a+r /etc/apt/keyrings/docker.gpg

#               echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

#               sudo apt-get update -y
#               sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
#               sudo usermod -aG docker ubuntu
#             fi

#             # 4. Setup directories
#             sudo mkdir -p /opt/marzban-production/nginx/conf.d
#             sudo chown -R ubuntu:ubuntu /opt/marzban-production
#             cd /opt/marzban-production

#             # 5. Nginx HTTP bootstrap (required for certbot)
#             cat << EOF > nginx/conf.d/marzban.conf
#             server {
#                 listen 80;
#                 server_name $APP_DOMAIN;

#                 location /.well-known/acme-challenge/ {
#                     root /var/www/certbot;
#                 }

#                 location / {
#                     return 200 "OK";
#                 }
#             }
#             EOF

#             # 6. Docker Compose
#             cat << EOF > docker-compose.yml
#             services:
#               marzban:
#                 image: $DOCKER_USER/marzban:latest
#                 restart: always
#                 ports:
#                   - "8000:8000"

#               nginx:
#                 image: nginx:alpine
#                 restart: always
#                 ports:
#                   - "80:80"
#                   - "443:443"
#                 volumes:
#                   - ./nginx/conf.d:/etc/nginx/conf.d
#                   - /etc/letsencrypt:/etc/letsencrypt
#                   - /var/www/certbot:/var/www/certbot

#               certbot:
#                 image: certbot/certbot
#                 volumes:
#                   - /etc/letsencrypt:/etc/letsencrypt
#                   - /var/www/certbot:/var/www/certbot
#                 entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait \$\${!}; done;'"
#             EOF

#             # 7. Start nginx (HTTP only)
#             sudo docker compose up -d nginx
#             sleep 10

#             # 8. SSL certificate request
#             sudo docker run --rm \
#               -v /etc/letsencrypt:/etc/letsencrypt \
#               -v /var/www/certbot:/var/www/certbot \
#               certbot/certbot certonly --webroot \
#               --webroot-path=/var/www/certbot \
#               -d $APP_DOMAIN \
#               --email $APP_EMAIL \
#               --agree-tos \
#               --no-eff-email \
#               --non-interactive

#             # 9. HTTPS nginx config
#             cat << EOF > nginx/conf.d/marzban.conf
#             server {
#                 listen 80;
#                 server_name $APP_DOMAIN;
#                 return 301 https://\$host\$request_uri;
#             }

#             server {
#                 listen 443 ssl http2;
#                 server_name $APP_DOMAIN;

#                 ssl_certificate /etc/letsencrypt/live/$APP_DOMAIN/fullchain.pem;
#                 ssl_certificate_key /etc/letsencrypt/live/$APP_DOMAIN/privkey.pem;

#                 location / {
#                     proxy_pass http://marzban:8000;
#                     proxy_set_header Host \$host;
#                     proxy_set_header X-Real-IP \$remote_addr;
#                     proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for;
#                 }
#             }
#             EOF

#             # 10. Deploy final stack
#             sudo docker compose pull
#             sudo docker compose up -d --remove-orphans
#             sudo docker compose restart nginx










name: End-to-End Infrastructure & App Deployment

on:
  push:
    branches: [ master ]

jobs:
  # STEP 1: Code Linting and Safety Verification
  test-and-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Run Syntax Checks
        run: |
          pip install flake8
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  # STEP 2: Terraform Infrastructure Provisioning
  infrastructure:
    needs: test-and-lint
    runs-on: ubuntu-latest
    outputs:
      vps_ip: ${{ steps.get_ip.outputs.vps_ip }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          cli_config_credentials_token: ${{ secrets.TF_API_TOKEN }}

      - name: Terraform Init & Apply
        env:
          TF_VAR_key_name: ${{ secrets.AWS_KEY_NAME }}
          TF_VAR_instance_name: "marzban-production-server"
          TF_VAR_instance_type: "t3.medium"
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
        run: |
          cd terraform-ec2/
          terraform init
          terraform apply -auto-approve

      - name: Extract VPS Public IP
        id: get_ip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}
        run: |
          cd terraform-ec2/
          echo "vps_ip=$(terraform output -raw public_ip)" >> $GITHUB_OUTPUT

  # STEP 3: Automated Docker Build
  build-image:
    needs: test-and-lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Multi-Arch Container
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ secrets.DOCKER_USERNAME }}/marzban:latest

  # STEP 4: Deployment + DuckDNS + SSL + App Bootstrapping
  deploy-and-secure:
    needs: [infrastructure, build-image]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Copy Stack Configurations to Target Host (Compose & Nginx)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ needs.infrastructure.outputs.vps_ip }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "docker-compose.yml,nginx/conf.d/marzban.conf"
          target: "/opt/marzban-production/"

      - name: Automated Bootstrapping via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          APP_DOMAIN: ${{ secrets.DOMAIN }}
          APP_EMAIL: ${{ secrets.EMAIL }}
          DOCKER_USER: ${{ secrets.DOCKER_USERNAME }}
          DUCK_TOKEN: ${{ secrets.DUCKDNS_TOKEN }}
          VPS_IP: ${{ needs.infrastructure.outputs.vps_ip }}
        with:
          host: ${{ needs.infrastructure.outputs.vps_ip }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: APP_DOMAIN,APP_EMAIL,DOCKER_USER,DUCK_TOKEN,VPS_IP
          script: |
            set -e

            # 1. Update DuckDNS
            SUBDOMAIN=$(echo $APP_DOMAIN | cut -d'.' -f1)
            echo "Updating DuckDNS..."
            curl -fsS "https://www.duckdns.org/update?domains=$SUBDOMAIN&token=$DUCK_TOKEN&ip=$VPS_IP"

            # 2. WAIT FOR REAL DNS PROPAGATION
            echo "Waiting for DNS propagation..."
            for i in {1..30}; do
              CURRENT_IP=$(nslookup $APP_DOMAIN | awk '/Address: / {print $2}' | tail -n1)
              echo "DNS resolved to: $CURRENT_IP"
              if [ "$CURRENT_IP" = "$VPS_IP" ]; then
                echo "DNS is correct ✔"
                break
              fi
              sleep 10
            done

            # 3. Install Docker if missing
            if ! command -v docker &> /dev/null; then
              sudo apt-get update -y
              sudo apt-get install -y ca-certificates curl gnupg dnsutils
              sudo install -m 0755 -d /etc/apt/keyrings
              curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
              sudo chmod a+r /etc/apt/keyrings/docker.gpg
              echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
              sudo apt-get update -y
              sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
              sudo usermod -aG docker ubuntu
            fi

            # 4. Setup production workspace paths safely
            sudo mkdir -p /opt/marzban-production/nginx/conf.d
            sudo mkdir -p /etc/letsencrypt/live/$APP_DOMAIN
            sudo mkdir -p /etc/letsencrypt/initial-fallback
            sudo mkdir -p /var/www/certbot
            
            sudo chown -R ubuntu:ubuntu /opt/marzban-production
            sudo chown -R ubuntu:ubuntu /etc/letsencrypt
            sudo chown -R ubuntu:ubuntu /var/www/certbot
            cd /opt/marzban-production

            # 5. Populate remote environment variables configuration file (.env)
            echo "DOCKER_USER=${DOCKER_USER}" > .env

            # 6. Inject dynamic application domain references directly into the tracked config
            sed -i "s|placeholder|$APP_DOMAIN|g" nginx/conf.d/marzban.conf

            # 7. Safe Initialization check: Seed mock certs ONLY into a clean fallback folder
            CERT_DIR="/etc/letsencrypt/live/$APP_DOMAIN"
            FALLBACK_DIR="/etc/letsencrypt/initial-fallback"

            if [ ! -f "$CERT_DIR/fullchain.pem" ]; then
                echo " Real SSL certificates not found. Creating temporary mock certificates..."
                openssl req -x509 -nodes -days 1 -newkey rsa:2048 \
                    -keyout "$FALLBACK_DIR/privkey.pem" \
                    -out "$FALLBACK_DIR/fullchain.pem" \
                    -subj "/CN=localhost"
                
                ln -sf "$FALLBACK_DIR/fullchain.pem" "$CERT_DIR/fullchain.pem"
                ln -sf "$FALLBACK_DIR/privkey.pem" "$CERT_DIR/privkey.pem"
            fi

            # 8. Spin up core architecture cleanly with the placeholder files
            echo " Launching core container ecosystem..."
            sudo docker compose pull
            sudo docker compose up -d marzban nginx --remove-orphans
            sleep 5

            # 9. Dispatch Certbot to issue real certificates
            echo " Requesting real Let's Encrypt validation keys..."
            sudo docker run --rm \
              -v /etc/letsencrypt:/etc/letsencrypt \
              -v /var/www/certbot:/var/www/certbot \
              certbot/certbot certonly --webroot \
              --webroot-path=/var/www/certbot \
              -d $APP_DOMAIN \
              --email $APP_EMAIL \
              --agree-tos \
              --no-eff-email \
              --non-interactive \
              --keep-until-expiring

            # 10. Seamlessly hot-reload Nginx configuration
            echo " Swapping mock elements for live authenticated lines..."
            sudo docker compose exec -T nginx nginx -s reload

            # 11. Final run sync status
            sudo docker compose up -d --remove-orphans
            echo "====== Pipeline Processing Cycle Completed Successfully ======"